"use strict";

let expect = require("expect");
let rewire = require("rewire");
let app = rewire("../index.js");

describe("createarticle tests", () => {

	//unit tests can be written using Mocha or Jest libraries.
});
